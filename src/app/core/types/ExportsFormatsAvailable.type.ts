/**
 * @typedef ExportsFormatsAvailable
 *
 * Represents the available export formats for data. This type restricts the export options to a predefined set of values.
 *
 * @type {'XLSX' | 'CSV'}
 *
 * @property {'XLSX'} XLSX - Represents the Excel spreadsheet format for exporting data.
 * @property {'CSV'} CSV - Represents the comma-separated values format for exporting data.
 */
export type ExportsFormatsAvailable = 'XLSX' | 'CSV';
