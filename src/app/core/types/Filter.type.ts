/**
 * @type FilterType
 *
 * Defines the possible filter types that can be applied to a column in a table or data set.
 * The filter can either be a 'dropdown' selection or a 'text' input, depending on the field's configuration.
 *
 * - 'dropdown': Allows the user to select a value from a predefined list.
 * - 'text': Allows the user to input a text string for filtering.
 */
export type FilterType = 'dropdown' | 'text';
